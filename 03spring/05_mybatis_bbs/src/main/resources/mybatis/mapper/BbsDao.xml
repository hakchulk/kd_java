<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.study.spring.bbs.IBbsDao">
	<select id="listDao" resultType="com.study.spring.bbs.BbsDto">
		select * from simple_bbs order by id desc
	</select>
	<select id="countDao" resultType="Integer">
		select count(*) from simple_bbs
	</select>	
	<select id="viewDao" resultType="com.study.spring.bbs.BbsDto">
		select * from simple_bbs where id=#{id}
	</select>
  <insert id="writeDao">
    INSERT INTO simple_bbs (writer, title, content)
    VALUES (#{param1}, #{param2}, #{param3})
  </insert>	
  
  <delete id="deleteDao">
  	delete from simple_bbs where id=#{id}
  </delete>
</mapper>